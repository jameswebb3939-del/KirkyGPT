apiVersion: v1

kind: Service

metadata:
  name: ec-pro-inference
  namespace: ec-pro

  labels:
    app: ec-pro-inference


spec:
  selector:
    app: ec-pro-inference

  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP

  type: ClusterIP