apiVersion: v1

kind: Service

metadata:
  name: ec-pro-inference
  namespace: ec-pro

  labels:
    app: ec-pro-inference-stub


spec:
  selector:
    app: ec-pro-inference-stub


  ports:
    - name: http

      protocol: TCP

      port: 8000

      targetPort: http


  type: ClusterIP